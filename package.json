{
  "name": "azinformatica/ssl-proxy",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "deploy": "docker build -t $npm_package_name:$npm_package_version . && docker tag $npm_package_name:$npm_package_version && docker tag $npm_package_name:latest && docker push $npm_package_name:$npm_package_version && docker push $npm_package_name:latest"
  }
}
